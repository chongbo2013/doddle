package com.dodles.gdx.dodleengine.editor;

/**
 * Marker interface to denote that the view should always be deactivated and
 * reactivated even if it's already active.
 */
public interface ForceReactivationEditorView {   
}
